server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/teaweb
    username: teaweb
    password: teaweb
  jpa:
    hibernate:
      ddl-auto: validate

app:
  cors:
    allowed-origins: "http://localhost:3000,http://localhost:5173"
